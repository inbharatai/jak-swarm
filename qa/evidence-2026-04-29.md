# Evidence — 2026-04-29

This file is the source-of-truth pointer to the recorded artifacts that
prove the work shipped this session. **Honest framing:** this is a
Playwright user-flow capture, NOT a Remotion narrated marketing video.
Remotion is multi-day infrastructure work and is honestly deferred (see
the bottom of `qa/claude-full-system-audit-2026-04-29.md`).

## Step 1 — StandingOrder UI panel (e2e smoke)

**Spec:** `tests/e2e/standing-orders.spec.ts`
**Run command:**

```bash
cd tests
PWHEADLESS=1 pnpm exec playwright test e2e/standing-orders.spec.ts --project=chromium-desktop
```

**Latest run (2026-04-29):** **2/2 passed** in 35.1s.

Tests prove:
- The page renders header + honest "How this works" banner + "New Standing Order" button + empty-state.
- Clicking "New Standing Order" reveals the create dialog with a name input + save button.

Both are real Chromium browser runs (not preview-tool eval) — confirms
the dev-server Suspense lockup observed via the preview tool was a
preview-tool-side stale state, NOT an app bug.

## Step 6 — Walk-through video + trace

**Spec:** `tests/e2e/evidence-recording.spec.ts`
**Run command:**

```bash
cd tests
PWHEADLESS=1 pnpm exec playwright test e2e/evidence-recording.spec.ts --project=chromium-desktop
```

**Latest run (2026-04-29):** **1/1 passed** in 5.0s.

**Artifacts (file paths):**

- `tests/test-results/evidence-recording-evidenc-99455-schedules-user-walk-through-chromium-desktop/video.webm` — 99 KB
- `tests/test-results/evidence-recording-evidenc-99455-schedules-user-walk-through-chromium-desktop/trace.zip` — 1.97 MB

**Convert to MP4 for sharing:**

```bash
ffmpeg -i video.webm -c:v libx264 -crf 22 video.mp4
```

**View the trace interactively (recommended):**

```bash
cd tests
pnpm exec playwright show-trace test-results/evidence-recording-*/trace.zip
```

**Walkthrough flow recorded:**
1. Land on the cockpit (`/`).
2. Navigate to `/standing-orders` (the new panel).
3. Open the create dialog (form fields visible).
4. Type a representative name into the name input.
5. Cancel the dialog.
6. Navigate to `/schedules` (the related surface).
7. Navigate back to the cockpit (round-trip).

## Step 4 — Tool audit harness output

**Run command:** `pnpm audit:tools`

**Latest run output:**

```
[audit:tools] total=122 pass=66 warn=56 fail=0
[audit:tools] wrote qa/all-tools-audit-report.md
[audit:tools] wrote qa/all-tools-audit-results.json
```

The 56 warnings are all the legacy `maturity: 'real'` alias — recognized
explicitly + flagged as a normalization opportunity, not a bug.

## Steps 2 + 3 — Unit-test summary

```
unit/api/workspace-lock.test.ts        8 tests passed
unit/skills/skill-cascade.test.ts      7 tests passed
unit/skills/skill-packs-validity.test.ts  10 tests passed (regression)
                                       ----
                                       25 / 25 green
```

## Live LLM bench run — exact one-liner (you run, not me)

`scripts/bench-runtime.ts --yc-wedge` is shipped + ready. Inside this
session I do not call OpenAI. To exercise the live path (cost
**$0.05–$0.20 per run**), set your key + run:

```bash
export OPENAI_API_KEY=sk-...
pnpm bench:runtime -- --yc-wedge --persona cmo --max-cost-usd 0.50
```

The harness writes a report to `qa/bench-runtime-<timestamp>.json`. If
you want me to read + summarize that file in a follow-up session, point
me at it.
